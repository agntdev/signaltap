[
  {
    "name": "Ignore reports unavailable storage clearly when the service is not configured",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Signal storage isn't set up yet. Please try again after the bot is configured."
            }
          }
        ],
        "send": {
          "callback": "signal:ignore:test-signal"
        }
      }
    ]
  }
]
